package pooJava.estruturaBasica;

public class Conceitos {

	public static void main(String[] args) {
		/*
		 * Estruturas básicas da OO:
		 * - Classe: É uma estrutura que abstrai um conjunto de objetos
		 * com características similares. Define o comportamento dos objetos
		 * por meio de métodos e os estados possíveis destes objetos por meio de atributos.
		 * Descreve os serviços providos por seus objetos e quais informações
		 * eles podem armazenar.
		 * Classe é nomeada como substativos, com nome significativos e o contexto deve ser considerado.
		 * 
		 * - Atributo: É o elemento de uma classe responsável por definir sua estrutura de dados.
		 * O conjunto destes será responsável por representar suas características e fará parte
		 * dos objetos criados a partir da classe. é um atributo da classe, que serve para representar
		 * de forma mais fidedigna os conceitos do mundo real.
		 * Atributo é o que é próprio e peculiar a alguma coisa, já
		 * Variável é sujeito a variações ou mudanças, que pode variar.
		 * Atributos pode ser caracterizado como substantivos e adjetivos, com nome significativos;
		 * Contexto deve ser considerado, abstração e tipos adequados.
		 * Não necessita definir tudo de inicio.
		 * 
		 * - Método: É uma porção de código que é disponibilizada pela classe. Ele é executado
		 * quando é feita uma requisição a ele. Um método serve para identificar quais serviços,
		 * ações, que a classe oferece. São resposnáveis por definir e realizar comportamentos.
		 * Método possui Visibilidade, Retorno, Nome e Parâmetros.
		 * É usado como Verbos, nome significativos e contexto deve ser considerado.
		 * >> Dois métodos especiais: Construtor e Destrutor;
		 * Construtor: Constrói objetos a partir da classe. Em Java é obrigatório ter o nome da Classe.
		 * Outra funcionalidade é definir parâmetros iniciais para os atributos por meio do Construtor.
		 * Destrutor: Auxilia a destrução do objeto; Em Java precisa do void finalize(){...};
		 * Sobrecarga: Mudar a assinatura (nome + parâmetros) de acordo com a necessidade.
		 * 
		 * - Objeto: É a representação de um conceito/entidade do mundo real, que pode ser física ou conceitual
		 * e possui significado bem definido para um determinado softare. Para esse conceito, deve ser definido uma classe
		 * e os objetos serão instancias de classes.
		 * 
		 * - Mensagem: É o processo de ativação de um método de um objeto. Ocorre quando uma requisição (chamada) a esse método
		 * é realizada, assim disparando a execução de seu comportamento descrito por sua classe.
		 * ou seja: Carro (Classe) carro (Variável) = new (Instanciando um objeto) Carro() (Construtor);
		 * carro.<método>; é a chamada do método;
		 * 
		 * 
		 * 
		 * 
		 */

	}

}
