package collectionsJava.map;

public class Map {
	
	/*
	 * java.util.Map;
	 * Elementos únicos (key) para cada valor (value);
	 * HashMap e LinkedHashMap possuem a mais perfomance e o TreeMap demora mais;
	 * HashMap itera os elementos aleatoriamente e o Linked é na ordem de inserção;
	 * 
	 */
	
}
