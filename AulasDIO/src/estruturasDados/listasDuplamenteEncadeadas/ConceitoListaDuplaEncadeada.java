package estruturasDados.listasDuplamenteEncadeadas;

public class ConceitoListaDuplaEncadeada {
	/*
	 * Tem uma referência para o próximo nó e para o nó anterior.
	 * Também existem duas referências, uma para o primeiro e outra
	 * para o último nó da lista.
	 * Métodos: .add(el), .add(index, el), .remove(index)
	 * .get(index)
	 */
}
