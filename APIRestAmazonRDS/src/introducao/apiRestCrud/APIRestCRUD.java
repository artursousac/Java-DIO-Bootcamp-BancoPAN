package introducao.apiRestCrud;

public class APIRestCRUD {

	public static void main(String[] args) {
		/*
		 * API faz o intermédio entre o cliente e a outra ponta.
		 * Garçom é uma API. Tendo Cliente e o Chefe.
		 * Padrão REST se encaixa com o protocolo HTTP: Comunicação entre API e Sistema.
		 * Possui regras, que é a arquitetura REST, modelo cliente servidor, onde o cliente
		 * só precisa saber da sua ponta e o servidor da sua ponta, sem precisar ter o conhecimento
		 * da outra ponta.
		 * Padrão REST é necessária as boas práticas, com regras bem definidas, comunicação entre sistemas e
		 * padrão de linguagem.
		 * Vantagens da padronização é a maior compreenção, redução do esforço, ganho em agilidade
		 * e eficiência e a migração de sistemas.
		 * Modelo Client-Server, Stateless (Não pode carregar o estado do outro lado), Cache
		 * Cache serve para aumentar a eficiência da rede.
		 * Uniform Interface é ter enfase de uma interface uniforme entre componentes, sendo generalista.
		 * Padrão comumente adotado, conversa bem com o protocolo HTTP, permite criação de APIs
		 * mais eficientes e possui foco no desenvolvimento.
		 * API Restful é quando segue o padrão REST.
		 * Modelo de maturidade de Richardson é dividido em 4 níveis, sendo o POX, Recursos
		 * Verbos HTTP e Hateoas.
		 * CRUD = Create (POST), Read (GET), Update (PUT) e Delete (DELETE). Operações básicas, persistência de dados, implementado
		 * em BD.
		 * 
		 * Modelagem de dominio vem da necessidade de compreensão do sistema.
		 * Modelagem especifica é a UML. Linguagem de OO e usada para especificação, construção,
		 * visualização e documentação.
		 * 02 realidades, sendo interpretada (perspectiva) e a construída (solução).
		 * Na UML existem visões, com diferentes pontos de vistas e aspectos particulares da aplicação.
		 * 
		 * 
		 */

	}

}
