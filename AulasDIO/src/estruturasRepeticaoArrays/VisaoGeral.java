package estruturasRepeticaoArrays;

public class VisaoGeral {

	public static void main(String[] args) {
		
		/* REPETIÇÃO:
		 * Cada repetição é chamada de laço (loop). Estrutura de repetição pode ser
		 * classificada como: while, do-while e for. Pode utilizar break para terminar
		 * de forma abrupta uma repetição. Quando o comando continue é executado, os
		 * comandos restantes da repetição são ignorados e o programa volta a testar
		 * novamente ou não.
		 * 
		 * 
		 * No comando "while", primeiro é verificado se a expressão é true ou false e
		 * realiza a sentença, retornando ao inicio do loop depois.
		 * 
		 * Já no comando "do-while", primeiro é executado a sentença e somente depois
		 * verificado a expressão, que se for true, retorna ao inicio e realiza a
		 * sentença novamente. em caso de false ele sairá do loop.
		 * 
		 * No comando For ele realiza um teste, realiza a sentença e no final faz uma
		 * atualização, retornando ao o teste e verificado se é true or false.
		 * 
		 * Operadores de Incremento e Decremento: ++numero (--numero): Soma uma unidade
		 * ao valor da variável. numero++ (numero--): Soma um ao valor da variável,
		 * retornando o valor original. São duas formas iguais.
		 * Outra forma é utilizar numero += k; é o mesmo que numero = numero + k;
		 * Pode, também, qualquer operador aritmético em vez do +.
		 * 
		 */
		
		/* ARRAYS:
		 * É um OBJETO utilizado para armazenar sequencialmente dados DO MESMO TIPO.
		 * Array permanece com o mesmo tamanho depois de criados.
		 * array (c). c[0] significa que o "c" é o nome do array e o [0] é o índice do elemento no array c.
		 * 
		 * a [0][0] significa um array "a" da linha 0 e na coluna 0.
		 * 
		 */

	}

}
