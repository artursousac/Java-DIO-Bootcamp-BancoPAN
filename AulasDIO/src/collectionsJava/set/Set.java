package collectionsJava.set;

public class Set {

	/*
	 * Não permite elementos duplicados.
	 * Não possui índice. (não possui get, set, indexof)
	 * HashSet não mantém a ordem dos elementos.
	 * LinkedHashSet mantém a ordem de inserção dos elementos.
	 * TreeSet mantém a ordem natural dos elementos.
	 * HashSet tem melhor perfomance quando comparado a Linked e Tree.
	 * HashSset e LinkedHashSet permitem no máximo 1 elemento null.
	 * TreeSet não permite elemento null.
	 * Implementa a interface Collection como no lists.
	 */
}
